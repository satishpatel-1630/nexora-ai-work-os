"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {api,IntelligenceRun} from "../../lib/api";
export default function IntelligencePage(){const [runs,setRuns]=useState<IntelligenceRun[]>([]);useEffect(()=>{void api.intelligenceRuns().then(setRuns).catch(()=>setRuns([]))},[]);return <main className="dashboard-shell"><section className="hero"><div><div className="eyebrow">INTELLIGENCE ENGINE</div><h1>Understand before execution.</h1><p className="hero-copy">Inspect NEXORA's structured intelligence runs, research, plans, specialist briefs, prompts and model usage.</p><Link className="button primary" href="/command">+ New intelligence run</Link></div></section><section className="panel"><div className="panel-head"><div><span className="eyebrow">RUN HISTORY</span><h2>Intelligence runs</h2></div></div>{runs.length===0?<div className="empty-state"><strong>No intelligence runs yet.</strong><p>Start from the Command page.</p></div>:<div className="project-list">{runs.map(run=><Link className="project-row" key={run.id} href={"/intelligence/"+run.id}><div className="project-icon">✦</div><div className="project-info"><strong>{run.request}</strong><span>{run.current_stage} · {run.status}</span></div><span className="badge">{run.status}</span><span className="arrow">→</span></Link>)}</div>}</section></main>}
