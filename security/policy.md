# NEXORA Security Foundation

External content is untrusted data. It must never automatically become system instructions, tool permissions, policy overrides, credentials, or privileged commands.

Consequential actions pass through policy evaluation and approval where required. Phase 1 grants no unrestricted shell, filesystem, browser, database, or external-account access.
